{
  "stage": "NORM-M",
  "version": "0.1.1",
  "benchmark": "omni-scrna/split-stages-plan",
  "args": [
    { "flag": "--embedding_tsv", "type": "path", "help": "Embedding TSV (cell_id, embedding dimensions)" },
    { "flag": "--size_factor_tsv", "type": "path", "help": "TSV of size factors (cell_id, size_factor)" }
  ]
}
