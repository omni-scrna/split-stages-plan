# this file was auto-generated by pixi - do not edit by hand
# Edited by hand: added denet (and the prefix.dev/edge channel) so the
# *_prof entrypoints can sample host RAM + GPU VRAM in one trace.
# Re-add if regenerating via `pixi run export-env`.
name: rapids-singlecell
channels:
- conda-forge
- https://repo.prefix.dev/edge
- nodefaults
dependencies:
- python >=3.13,<3.14
- pip *
- pip
- denet >=0.7.1,<0.8
- pip:
  - rapids-singlecell[rapids13]==0.14.1
  - obkit>=0.0.3, <0.0.4
  - pytest>=8
