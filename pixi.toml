[workspace]
name = "split-stages-plan"
version = "0.1.0"
channels = ["conda-forge"]
platforms = ["linux-64"]

[feature.validation.tasks]
validation = "validators/all.sh"

[environments]
validation = { features = ["validation"] }

[feature.validation.dependencies]
r-readr = "*"
r-tibble = "*"
